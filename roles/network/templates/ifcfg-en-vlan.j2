# Generated by ansible-awesome

VLAN=yes
DEVICE={{ interface }}.{{ item.vlan }}
BOOTPROTO=static
ONBOOT=yes
IPADDR={{ item.ipv4 | ipaddr('address') }}
NETMASK={{ item.ipv4 | ipaddr('netmask') }}
NETWORK={{ item.ipv4 | ipaddr('network') }}
BROADCAST={{ item.ipv4 | ipaddr('broadcast') }}
{% if item.gateway is defined %}
GATEWAY={{ item.gateway }}
{% else %}
DEFROUTE=no
{% endif %}
{% if network_interface.name is defined %}
NAME={{ network_interface.name }}
{% endif %}
NM_CONTROLLED=no
