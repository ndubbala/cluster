---

- name: "Install Packages"
  yum:
    pkg: "{{ item }}"
    state: present
  with_items:
    - "epel-release"

- name: "Install Packages"
  yum:
    pkg: "{{ item }}"
    state: present
  with_items:
    - "iotop"
    - "mosh"
    - "dos2unix"
    - "unix2dos"
    - "nano"